# SQLite WAL/SHM — archivos temporales, no versionados
# (clt.db SÍ se versiona: el workflow diario la necesita para mantener histórico)
scraper/clt.db-shm
scraper/clt.db-wal

# Entorno virtual Python
scraper/.venv/

# Bytecode de Python (temporal, lo genera el intérprete)
__pycache__/
*.pyc

# Next.js
frontend/.next/
frontend/node_modules/
frontend/.env*.local

# macOS
.DS_Store
.vercel
